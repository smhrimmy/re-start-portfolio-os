import { ThemeConfig } from '../types';
import Theme09Component from './index';

export const theme09Config: ThemeConfig = {
  id: 'theme-09',
  name: 'ERA SELECT // GTA PORTFOLIO OS',
  description: 'Franchise-wide GTA diegetic pause menu portfolio with 6 era re-skins (III, Vice City, San Andreas, IV, V, VI), loading screen wipe transition, wanted-level skill stars, and real-time palette & typography swaps.',
  version: '1.0.0',
  author: 'Prajwal DL',
  supports3D: false,
  minSupportedTier: 'LOW',
  component: Theme09Component,
};

export default theme09Config;
