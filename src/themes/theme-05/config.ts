import { ThemeConfig } from '../types';
import { Theme05Component } from './index';

export const theme05Config: ThemeConfig = {
  id: 'theme-05',
  name: 'Creative Developer Editorial',
  description: 'Two-tone cream & noir editorial developer portfolio featuring liquid SVG distortion, hand-drawn signature preloader, and GSAP motion interactions.',
  version: '1.0.0',
  author: 'Portfolio OS Core',
  supports3D: false,
  minSupportedTier: 'LOW',
  component: Theme05Component,
};

export default theme05Config;
