import { ThemeConfig } from '../types';
import { Theme06Component } from './index';

export const theme06Config: ThemeConfig = {
  id: 'theme-06',
  name: 'Ethereal // 3D',
  description: 'Premium glassmorphism + 3D light minimal portfolio with scroll-driven animations, floating 3D elements, and cinematic transitions.',
  version: '1.0.0',
  author: 'Portfolio OS Core',
  supports3D: true,
  minSupportedTier: 'MEDIUM',
  component: Theme06Component,
};

export default theme06Config;
