import { ThemeConfig } from '../types';
import { Theme01Component } from './index';

export const theme01Config: ThemeConfig = {
  id: 'theme-01',
  name: 'Minimal Editorial',
  description: 'Premium editorial publication theme featuring magazine layouts, chapter transitions, serif display typography, and archival plates.',
  version: '1.0.0',
  author: 'Portfolio OS Core',
  supports3D: false,
  minSupportedTier: 'LOW',
  component: Theme01Component,
};

export default theme01Config;
