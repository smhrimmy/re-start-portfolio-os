import { ThemeConfig } from '../types';
import Theme08Component from './index';

export const theme08Config: ThemeConfig = {
  id: 'theme-08',
  name: 'PAUSE MENU // OS',
  description: 'Diegetic game pause menu portfolio with 100dvh layout, live persistent HUD, 9 tab art plates, sliding Framer Motion selection cursor, and 14px corner-cut bevel styling.',
  version: '1.0.0',
  author: 'Prajwal DL',
  supports3D: false,
  minSupportedTier: 'LOW',
  component: Theme08Component,
};

export default theme08Config;
